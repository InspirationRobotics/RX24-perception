from setuptools import setup, find_packages

setup(
    name='lidar_core',
    version='0.1',
    description='Core lidar package for Inspiration RobotX',
    author='Eesh Vij',
    packages=find_packages(include=['lidar_core'])
)